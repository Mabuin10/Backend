<div>
  <h1>Lista de Productos</h1>
  <ul>
    {{#each products}}
      <h2>{{this.title}}</h2>
      <p>{{this.description}}</p>
      <p>{{this.category}}</p>
      <p>{{this.price}}</p>
      <p>{{this.stock}}</p>
    {{/each}}
  </ul>
</div>